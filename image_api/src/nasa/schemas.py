"""Nasa schemas."""
from enum import Enum


class ModelImageNasa(Enum):
    """Model of path for nasa images."""

    title = "title"
    explanation = "explanation"
    date = "date"
    url = "url"
